package commbot

// Add places a new link into the Pocket app
func Add(link string) (int, error) {
	//url := environment["POCKET_URL"]
	//key := environment["POCKET_CONSUMER_KEY"]
	//token := environment["POCKET_ACCESS_TOKEN"]

	//req, err := http.NewRequest("POST", url, bytes.NewBufferString(""))
	//if err != nil {
	//	return 0, err
	//}

	return 0, nil
}

// Archive places the specified item into the "read" list in Pocket
func Archive(itemID int) (int, error) {
	return 0, nil
}
